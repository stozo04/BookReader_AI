// Copy this file to src/environments/environment.local.ts and fill in values for local development.
// Do NOT commit environment.local.ts - it is ignored by .gitignore.
export const environment = {
  production: false,
  // If you want to use the live Gemini API locally, put your key here. Leaving empty will enable dev mocks.
  GEMINI_API_KEY: '',
  // Supabase client URL and anon key - for development you can use your project's anon key if it's intended for client use.
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
};
